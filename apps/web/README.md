# @clip2sticker/web

This directory is reserved for the first-party browser UI.

The product direction for this repository is:

- `apps/web` ships the official sticker creation experience.
- `packages/ffmpeg` remains separately consumable for people who want to build their own UI on top of the same browser-side FFmpeg layer.

No application code has been moved here yet.
